# FDS_CURRENT_STATE

## Canonical status — 2026-08-17

`ALPHA_PASS=0`.

Historical measured V25/V26 execution remains frozen behind missing exact recovered-runtime bytes. Active work is mathematics-first exact Walsh/tensor algebra. Code is only a calculator/falsifier; admitted finite claims require clean-checkout execution.

## One-QR exact authority

For all4096 physical fixed-mask cases:

`W_1<=38.768184324776925...<39`, sharp peak `218*2^31`.

Old support216 / 13 support classes / rank12 selector is revoked. Exact I9 support is117..218 over64 left masks; support-indicator rank64; all64 left maps share an exact48-dimensional interface row space. `epsilon=0`.

## d=1 canonical ledgers

- topology-only `W_2_topo<=236`;
- coefficient-blind static method `W_static,blind=95`;
- exact representation existence
  `W_repr(1)<=73+log2(3829)=84.90275194485017...`;
- coefficient-aware materialized-factor generation
  `W_factor-gen<=84.90275194485017...`;
- arithmetic-work reduction: **not admitted**.

## Full S1 center

`S1={0,1,2,3,4,5,12,13,14,15,16}`.

All44 S1 physical row bits are occurrence-closed.

Low23: `A0..5,B0..5,D0..5,C12..16`.  
High/gap21: `A12..16,B12..16,D12..16,C0..5`.

Exact structure:

- D0..5 low repeated-D rank `2^19`, clean `32041446052`;
- C12..14 universal carry rank16/16, low rank `n=2^22`, clean `32040887484`;
- j2 bits22..31 bridge rank32/32, clean `32041617140`;
- j2 bits1..11 rank66/128, kernel62, clean `32042410012`;
- fixed-D16 minimal loop misses kernel, clean `32042794811`;
- D16 after C1..5: `64,64,intersection63`, uniform over `q=(v3_12,sigma2_11)`, clean `32043345651`;
- j1 D16: `448,448,intersection424`.

Per high prefix `30632*n`;16 prefixes direct:

`rank_center(S1)<=3829*2^29=2,055,678,722,048`.

Clean full run `32043410513`.

With predecessor-leaf exponent44:

`dim_factor<=3829*2^73`,

`W_repr(1)<=84.90275194485017...`.

Factor-generation constructivity matches this message/storage bound; clean `32043554316`.

## d=1 work gates

D16 per-prefix dimension decomposition:

`26712n common +1960n private0 +1960n private1`.

Explicit factor materialization emits

`3829*2^73 =36,163,882,525,815,743,046,483,968`

scalars; clean materialization gate `32044123499`.

Low/high full-star mode envelopes are `2^45` and `3829*2^28`; their Cartesian product exactly recreates `3829*2^73`. Clean `32055881609`.

Monotone complement interleaving with raw central extension/topology and only generic leaf Hilbert ranks cannot certify below84.90275. At the unavoidable44-added-row layer the coefficient certificate is85.90275194485 and exact topology+leaf MILP optimum is86. Clean `32056604049`.

Retained-only j1 gap6..11 is rank4/4, clean `32043801142`.

## Predecessor-leaf exact coefficient structure

The reduced exact leaf topology has terminal cut11 on every critical frozen HT partition; topology alone cannot improve the generic `2^11` single-leaf rank. Clean historical run `32010227963`.

The old dense coefficient full-rank workflow `32011941759` is non-authoritative: exit143 before result. Exploratory 2048x2048 single-copy minor run `32054802994` is also non-authoritative: greedy path required a `2^64` intermediate and failed on32-GiB allocation before rank output.

### Formal top-carry cancellation

A four-addition predecessor leaf has formal carry denominator exponent124.

Exact GF(2) carry/sign algebra proves:

- A/D weight124 impossible;
- B/C weight124 internal rank124/128, nullity4;
- sign polar rank2;
- every affine-fiber Gauss sum `0,+8,-8`.

Thus formal `2^-124` is never needed. Clean `32057838229`.

### Position-dependent dyadic denominator

A/D: j4 output is identically zero, forcing every j4 carry bit to zero. Maximum carry weight becomes93. Its unique top sector has internal rank127 and two-point fibers, hence even signed numerator:

`L_A(beta),L_D(beta) in 2^-92 Z`.

B/C:

- weight123 `(rank,nullity,polar)` classes:
  `(124,4,2):95`, `(125,3,2):14`, `(125,3,0):7`, `(126,2,0):8`;
- weight122 internal ranks:
  `124:4465,125:2058,126:1025,127:74,128:4`;
- the four rank128 sectors are precisely the bit0 pairs
  `(j1,j2)`, `(j1,j4)`, `(j2,j3)`, `(j3,j4)`;
- those four leave one identical rank8 external consistency system for B, and likewise for C.

Therefore

`L_B(beta),L_C(beta) in 2^-121 Z`.

Clean run `32058413111`: success.

Authority:

- `V26_Q138_PREDECESSOR_LEAF_TOP_CARRY_CANCELLATION.md`;
- `V26_Q138_PREDECESSOR_LEAF_DYADIC_DESCENT92_121.md`.

### A/D first 2-adic parity layer

Set

`M_A=2^92 L_A`, `M_D=2^92 L_D`.

Modulo2, only three affine-indicator contributions survive:

1. weight92 `(j1 bit0)`;
2. weight92 `(j3 bit0)`;
3. weight93 two-point fiber with affine sign-derivative-equals-zero condition.

For each indicator, exact internal elimination gives zero intersection between the S1 beta-column image and its complement image. Each indicator therefore has GF(2) communication rank1.

Hence uniformly over fixed external input masks:

`rank_F2((2^92 L_A) mod2 across S1)<=3`,

`rank_F2((2^92 L_D) mod2 across S1)<=3`.

Clean run `32058931522`: success.

Authority:

- `V26_Q138_PREDECESSOR_LEAF_AD_PARITY_RANK3.md`;
- `scripts/verify_v26_q138_predecessor_leaf_ad_parity_rank3.py`.

This is a first 2-adic residue result, **not** rational Schmidt rank<=3.

It opens an exact layered representation route:

`2^92 L = M0 + 2 M1`, with `rank_F2(M0)<=3` for A/D.

No canonical frozen `outer128` predecessor mask is available; source-specific leaf claims remain provenance-gated.

## Closed / revoked d=1 routes

- block2 D11 one-bit extension: no gain;
- contiguous C12..C14 alone: no gain;
- repeated-D D0..5 alone: no gain;
- direct shared `v3_12` alone: product exact;
- 39-bit natural bridge before C1..5: old center exactly;
- fixed-D16 minimal gap loop: no gain;
- j1 retained gap6..11: no gain;
- explicit materialization, separate full low/high bases and generic monotone complement interleaving: scoped work NO-GAIN routes;
- old four-site ranks96/208 and W84/W83 claims revoked due int32 overflow.

## d>=2 authority — unchanged

Frozen `S3={4,5,11,12,13,19,20,21,27,28,29}`.

Fully-open exponent63.562242424221076. Semi-open A54.661778097771986, B generic55, C54.92481250360578, D53.74357218893564.

`W_repr(d)<=508.4979393937686...d-333.8951148057971...`, d>=2.

## Current sharp blockers / next mathematics

1. **A/D 2-adic lifting:** compute the next residue `M1 mod2` after removing the rank<=3 first layer. Determine whether successive exact residue ranks remain uniformly small.
2. **B/C first 2-adic parity object:** use the `2^121` scaling plus the finite carry-sector/Gauss classification to construct the exact S1 parity-Schmidt representation.
3. **Leaf-central joint evaluator:** only after compact leaf layers exist, fuse them into the D16 `common+private+private` central decomposition before a full low/high Cartesian basis forms.
4. Other nonlocal center regroupings, semi-open B<55 and fully-open S3<63.562 remain separate exact problems.
5. Clean-checkout every finite claim before authority changes.

All admitted reductions remain exact, `epsilon=0`. Approximation remains inactive while exact routes remain open.

Still not admitted: practical evaluator, arithmetic-work reduction, ranking/search reduction, `alpha<1`, or full-round relevance.
