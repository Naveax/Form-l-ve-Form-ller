# FDS_CURRENT_STATE

## Canonical status — 2026-08-17

All previously killed/inapplicable V25/V26 families remain closed and `ALPHA_PASS` remains 0.

The recovered bit-puncturing/D&C Stage0 remains admitted as `PASS_EXACT_SINGLE_COLUMN_SEPARATOR_STAGE0`. Its cap4 packed single-column separator supports were `[0,11846464,26912,0]`; these remain exact **inside the frozen bounded trail family**, not a complete full-exact joint second-layer object.

The active work while historical measured-runtime provenance is blocked is mathematics-first exact Walsh tensor-network analysis. Code is only a calculator/falsifier.

## Exact Walsh structural chain

Established source-independent/local results include:

- exact modular-addition Walsh MPO/TT representation with binary longitudinal sigma/carry bond dimension 2;
- exact optimal local transverse TT profile `[2,3,3,2]` for one interior five-leg addition-bit tensor;
- exact semantic ternary basis `00 / 11 / unequal` for the rank-3 local states;
- class-parity conservation `e(c) xor e(d)=s` and its telescoping form;
- full-exact Parseval/orthogonality, residual-Gram, epsilon-rank and local-to-global error machinery.

For the q138 fixed-output one-quarter-round exact Walsh object, the certified global dense-message upper-bound chain remains

`44 -> 42 -> 41 -> 40`.

Interpretation:

- `44`: natural fused-site path;
- `42`: optimal fused-site carving tree;
- `41`: exact minimal-TT-core refinement;
- `40`: exact q138 rank-528 separator compression followed by a complete 536-leaf / 535-internal-node contraction-tree certificate.

Canonical width-40 authority:

- `research/v26/recovered-bit-puncturing-dac/V26_QR_Q138_ALGEBRAIC_WIDTH40_THEOREM.md`
- `research/v26/recovered-bit-puncturing-dac/V26_QR_Q138_ALGEBRAIC_WIDTH40_CERTIFICATE.json`
- `scripts/verify_v26_qr_q138_algebraic_width40.py`

`W <= 40` is an exact message-dimension upper bound for the q138 fixed-output one-QR representation. It is not an optimality theorem, not an arithmetic-work bound, and not a generic arbitrary-mask/double-round result.

## New width-40 bottleneck algebra

The unique `2^40` maximum cluster contains 171 leaves and splits `64|107` across six binary interface bits. Its 40 parent-external bits partition `16|24`.

Topology alone therefore gives

`rank(parent 16|24 flattening) <= 64`.

The 64-leaf child can already be constructed with peak message `2^22`.

### Uniform left-child theorem

The 64-leaf child map

`L_64 : 2^16 -> 2^6`

has exact rational rank

`rank(L_64)=48`.

This was checked for **all 64 combinations** of the six relevant fixed T1/T2 input-mask bits using exact dyadic/rational local Walsh tensors and exact Gaussian elimination. Affine XOR/parity constants are internal index-state relabelings and do not change the map rank.

Authority:

- `research/v26/recovered-bit-puncturing-dac/V26_QR_Q138_WIDTH40_LEFT_CHILD_RANK48_THEOREM.md`
- `scripts/verify_v26_qr_q138_width40_left_rank48.py`

Thus the six-bit topology interface has a uniform exact algebraic compression

`64 -> 48`.

A first natural rank-48 reinsertion geometry still reaches a residual 240-factor subproblem with no split whose two child boundaries are both <=39 in that **specific** geometry. This is not a global width-39 lower bound or NO-GO.

### Right-map rank is input-mask conditioned

The opposite 107-leaf map

`R : 2^6 -> 2^24`

does **not** have one mask-independent exact rank.

Exact doubled-network Gram contractions give concrete fixed-mask examples:

- zero-gauge representative: `rank(R)=34`, exact parent rank `rank(LR)=23`;
- all-one representative: `rank(R)=19`, parent rank `8`;
- another exact representative: `rank(R)=37`, parent rank `26`.

The left child remains rank 48 in all those cases.

Authority:

- `research/v26/recovered-bit-puncturing-dac/V26_QR_Q138_RIGHT_MAP_RANK_CONDITIONING_FALSIFIER.md`
- `scripts/verify_v26_qr_q138_right_map_rank_conditioning.py`

Therefore zero-gauge numbers such as `right rank 34` or `parent rank 23` must **not** be universalized to all fixed input masks.

This is a mathematically important transition: the next compression layer is naturally **source/input-mask conditioned**.

## Current sharp mathematical bottleneck

The active problem is no longer blind graph ordering and no longer simply “replace 64 by one unconditional rank.”

Define, for the fixed-input mask class `m`,

`rho_R(m) = rank R_m`

and

`rho_parent(m) = rank(L_m R_m)`.

The next exact pass is:

1. identify which fixed input-mask bits actually control the 107-leaf/right-map rank rather than acting only as gauge/relabeling variables;
2. derive a compact sector/class description of `rho_R(m)` or directly `rho_parent(m)`;
3. restrict that rank law to the mask family that is genuinely reachable/relevant for the q138 source-conditioned contraction;
4. prove either a uniform envelope on that relevant family or a finite class decomposition with one exact rank bound per class;
5. only then reinsert conditioned rank factors globally and require a complete machine-verifiable tree/work certificate before claiming `W<=39` or another work reduction.

A brute-force scan over all arbitrary fixed masks is not the preferred mathematical mechanism. Use theorem -> sector law -> finite falsifier.

Only after exact conditioned-rank structure stalls should certified epsilon-rank/tail approximation become the active mechanism.

## Active measured QR-transform falsifier

`V26_SINGLE_COLUMN_QR_TRANSFORM_FALSIFIER_PLAN.json` remains frozen. No new canonical historical project-cap measurement is admitted while provenance is missing.

## Current measured-execution blocker

The exact recovered `fds_v25_bit_puncturing.py` runtime source is still absent.

Canonical recovery authority:

- `research/v26/recovered-bit-puncturing-dac/V26_MISSING_CANONICAL_BYTES.md`
- GitHub Issue #22

Required identities:

- snapshot SHA-256 `fd4d1fbf2378b7950430f18f9efb49f2dab875ee1f72bea5a0336c9d1c5180b6`
- core SHA-256 `ec81640f87aaaa97ec5805a973a282241e9e2c2b86011530b4db519dec2be130`

This remains a provenance blocker, not a mathematical NO-GO.

After byte recovery: SHA-locked rematerialization -> prove exact historical cap semantics -> historical `19/19 PASS` -> frozen cap2 all-column QR regression -> only PASS opens cap3 under the existing resource gates.

Still not admitted: practical exact QR evaluation, exact end-to-end arithmetic-work reduction, full second-layer contraction, four-inverse-round exact energy, ranking reduction, `alpha<1`, or full-round relevance.
