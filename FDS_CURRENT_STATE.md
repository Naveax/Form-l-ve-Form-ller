# FDS_CURRENT_STATE

## Canonical status — 2026-08-17

`ALPHA_PASS=0`.

Historical measured V25/V26 execution remains frozen behind missing exact recovered-runtime bytes. Active progress is mathematics-first exact Walsh/tensor algebra. Code is only a calculator/falsifier; finite admitted claims require clean-checkout execution.

## One-QR exact authority

For all4096 physical fixed-mask cases:

`W_1<=38.768184324776925...<39`, sharp peak `218*2^31`.

Old support216 / 13 support classes / rank12 selector is revoked. Exact I9 support is117..218 over64 left masks; support-indicator rank64; all64 left maps share an exact48-dimensional interface row space. `epsilon=0`.

## d=1 canonical ledgers

Keep separate:

- topology-only `W_2_topo<=236`;
- coefficient-blind static graph+leaf method `W_2_static,blind=95`;
- exact representation existence
  `W_repr(1)<=73+log2(3829)=84.90275194485017...`;
- coefficient-aware materialized-factor generation
  `W_factor-gen<=84.90275194485017...`;
- arithmetic-work reduction: **not admitted**.

Old exact representation/factor-generation value `85.4178525148859...` is superseded. Exact improvement `0.5151005700357...` bits.

## Full S1 exact decomposition

`S1={0,1,2,3,4,5,12,13,14,15,16}`.

All44 physical S1 row bits are occurrence-closed.

Low23 rows:

`A0..5,B0..5,D0..5,C12..16`.

High/gap21 rows:

`A12..16,B12..16,D12..16,C0..5`.

### Low / high bridges

- repeated-D D0..5 rank `2^19`, clean `32041446052`;
- C12..14 carry map rank16/16, giving low rank `n=2^22`, clean `32040887484`;
- j2 bits22..31 bridge rank32/32, preserving all16 D12..15 prefixes, clean `32041617140`.

### Useful j2 gap overlap

j2 bits1..11 has row domain128, exact rank66, kernel62; clean `32042410012`.

Fixed-D16 minimal loop misses that kernel; clean `32042794811`.

Keeping D16=0/1 together after closing C1..5 gives exact physical sector geometry

`64,64,intersection63`,

uniform in every fixed shared `q=(v3_12,sigma2_11)` slice. Thus incoming row-space dimension n lifts to

`64n,64n,intersection63n`.

Clean `32043345651`.

Extended-block2 j1 D16 geometry is `448,448,intersection424`, so each D12..15 high prefix has rank

`30632*n`.

Sixteen prefixes are direct:

`rank_center(S1)<=16*30632*2^22=3829*2^29=2,055,678,722,048`.

Clean full rank/tree run `32043410513`.

With predecessor-leaf exponent44:

`dim_factor<=3829*2^73`,

`W_repr(1)<=84.90275194485017...`.

## Factor-generation constructivity

Streaming exact Gaussian can select actual physical S1 row-basis elements with pivot storage exponent at most81.8055. The physical-row to rank-coordinate transform has at most `3829*2^73` entries and the21-site complement entry tree remains peak80.

Thus

`W_factor-gen<=84.90275194485017...`.

Clean `32043554316`.

This is message/storage constructivity only.

## Secondary representation closure

Retained-only j1 sites6..11 carry gap has endpoint domain4 and exact Gram rank4/4. Direct route is injective, no gain below `3829*2^29`.

Clean `32043801142`.

## Arithmetic-work structure

### D16 common/private decomposition

Per high prefix:

`26712n common +1960n private0 +1960n private1 =30632n`.

About87.2% of the D16 union dimension is common.

### Explicit materialization gate

A complete coefficient-aware factor contains

`3829*2^73 =36,163,882,525,815,743,046,483,968`

scalars. Explicit materialization therefore has scalar-output exponent84.90275 and is scoped NO-GO as the route to a work win.

Clean `32044123499`.

### New: low/high star product barrier

For the complete five-QR star, generic predecessor-leaf Hilbert bounds give

- low23 mode `<=2^22 *2^23 =2^45`;
- high21 mode `<=490112*2^21 =3829*2^28`.

Their Cartesian product is exactly

`2^45*(3829*2^28)=3829*2^73`.

So independently completing low and high channel bases before joining them recreates the current materialization wall exactly. A useful evaluator must remove degrees of freedom before this joint is formed.

Clean `32055881609`.

Authority:

- `research/v26/recovered-bit-puncturing-dac/V26_Q138_D1_LOW_HIGH_STAR_PRODUCT_BARRIER.md`;
- `scripts/verify_v26_q138_d1_low_high_star_product_barrier.py`.

### New: monotone complement-interleave generic-leaf barrier

Start from high21 and monotonically add the84 non-S1 physical rows one at a time. At each state use either

- high central rank extended by raw `x2` per added row, or
- exact reduced central topology terminal cut,

plus only generic predecessor-leaf Hilbert ranks.

Every such path visits the layer with44 added rows.

At that layer the generic four-leaf Hilbert exponent has exact minimum23. Therefore the coefficient/raw-extension certificate is at least

`log2(490112)+44+23 =85.90275194485016...`.

A clean binary cut MILP over every choice of44 complement physical rows gives exact topology-plus-generic-leaf optimum86, with matching dual bound86 and zero MIP gap.

Hence this entire monotone/raw-extension/generic-leaf certificate class cannot certify below the current84.90275. This is a method-scope NO-GAIN result, not a true-rank or unrestricted-work lower bound.

Clean `32056604049`.

Authority:

- `research/v26/recovered-bit-puncturing-dac/V26_Q138_D1_MONOTONE_COMPLEMENT_INTERLEAVE_NO_GAIN.md`;
- `scripts/verify_v26_q138_d1_monotone_complement_interleave_no_gain.py`.

## Predecessor-leaf status

The reduced exact leaf topology has terminal cut11 on all frozen HT critical partitions. Thus topology alone cannot improve the generic `2^11` single-leaf rank bound. Clean historical run `32010227963`.

The old coefficient full-rank witness run `32011941759` is non-authoritative: runner shutdown/exit143 occurred before a mathematical result.

A new exploratory single-copy 2048x2048 minor run `32054802994` is also non-authoritative. Its generic greedy contraction predicted a `2^64` intermediate and failed on a32-GiB allocation before any rank result. This is a contraction-path failure, not evidence for low or full leaf rank.

No canonical frozen `outer128` predecessor mask was found in the repository. Source-specific predecessor-leaf coefficient claims therefore remain provenance-gated. A uniform theorem is still admissible.

## Closed / revoked d=1 routes

- block2 D11 one-bit extension: no gain;
- contiguous C12..C14 alone: no gain;
- repeated-D D0..5 alone: no gain despite local kernels;
- direct shared `v3_12` alone: product bound exact;
- 39-bit natural bridge before C1..5: old center exactly;
- fixed-D16 minimal gap loop: no gain;
- j1 retained gap6..11: rank4/4;
- explicit full-factor materialization: scoped arithmetic NO-GO;
- separate full low/high basis Cartesian joint: scoped arithmetic NO-GO;
- monotone raw complement interleaving with generic leaf Hilbert ranks: method-scope NO-GAIN;
- old four-site ranks96/208 and W84/W83 claims are **revoked** due int32 overflow; corrected ranks256/256.

## d>=2 authority — unchanged

Frozen `S3={4,5,11,12,13,19,20,21,27,28,29}`.

Fully-open exponent63.562242424221076.

Semi-open A54.661778097771986, B generic55, C54.92481250360578, D53.74357218893564.

Current law for all d>=2:

`W_repr(d)<=508.4979393937686...d-333.8951148057971...`.

## Current sharp blockers / next mathematics

1. **d=1 coefficient-specific or uniform leaf-central joint algebra.** Generic leaf Hilbert accounting is now the identified work blocker. Preserve the D16 common/private decomposition and contract actual predecessor-leaf coefficients before a full low/high joint basis is formed.
2. **Uniform predecessor-leaf 11|21 coefficient rank.** Leaf topology cut11 is saturated topologically but coefficient saturation is unresolved. Replace failed dense/greedy Gram/minor attempts by a small exact streaming, sparse, or direct algebraic channel certificate. Source-specific work requires a real frozen outer128 mask.
3. **Other nonlocal center regroupings.** Further simultaneous retained-coordinate couplings may still beat `3829*2^29`.
4. **Semi-open B<55** and **fully-open S3<63.562** remain separate exact d>=2 problems.
5. Clean-checkout every finite claim before authority changes.

All admitted reductions remain exact, `epsilon=0`. Approximation remains inactive while exact routes remain open.

Still not admitted: practical evaluator, arithmetic-work reduction, ranking/search reduction, `alpha<1`, or full-round cryptanalytic relevance.
