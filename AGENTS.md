# AGENTS.md — FDS Autonomous Continuation Contract

1. Resolve fresh main and authority.
2. Issues #10–#14 are closed NO-GO and immutable.
3. Current family exhausts every nonzero linear XOR projection over all 16 boundary words.
4. Use exact GF(2) equation spans; do not brute-force arity variants or drop equations.
5. Stable masks must satisfy forward and all four backward opposite-half constraints.
6. Approximate nullspace and target-specific masks are forbidden.
7. Numerical MITM/TOTAL opens only for stable nonzero masks passing match-count gates.
8. Local compute allowed; authority returns to GitHub.
9. Structural PASS is not alpha<1 until fresh TOTAL scaling.
10. No full-round claim without independent gates.
