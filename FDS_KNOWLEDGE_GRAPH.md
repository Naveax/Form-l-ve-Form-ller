# FDS_KNOWLEDGE_GRAPH

boundary syndrome
→ broad QR layout transfer 5/5
→ generic cache wall transfer NO-GO
→ straight-line codegen
   → exactness PASS
   → wall transfer W5/W6/W7/W4+W6 PASS
   → same-column W4+W8 wall FAIL
→ scaling remained constant-factor / alpha=1

recovered bit-puncturing / trail D&C
→ first-layer exact factorization PASS_WITH_LIMITS
   → cap5 43,071,961,472 raw trails vs 3,438,542 local entries (~12,526x theoretical compact-memory reduction)
→ historical later second-layer/CERT90/ranking chain recovered
   → useful exact-within-cap contraction machinery
   → no robust end-to-end ranking/alpha win
→ V26 exact single-column separator Stage0
   → cap2/cap3 exact all-column validation PASS
   → cap4 global Cartesian avoided
   → supports `[0,11846464,26912,0]`, ~285 MB compact, ~839 MiB RSS
   → `PASS_EXACT_SINGLE_COLUMN_SEPARATOR_STAGE0`
→ active single-column inverse-QR falsifier
   → plan frozen
   → cap2 factorized/reference implementation + regression committed
   → BLOCKED before measurement: exact recovered `fds_v25_bit_puncturing.py` bytes not present in current Git/mount
   → next: rematerialize SHA-verified core, rerun 19/19, then cap2 → packed cap3
   → pairwise/junction-tree joint contraction only after PASS

Global claim boundary: `ALPHA_PASS=0`; no full second-layer, four-inverse-round exact energy, end-to-end work reduction, or full-round relevance is admitted.
