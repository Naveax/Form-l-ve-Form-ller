# V26 Q1.38 B direct-e1 aggregate extended mod-3 probe

The clean complete direct-e1 aggregate mod-3 diagnostic at the deterministic reachable B predecessor reached rank1536 after4096 Gray-code columns and was still increasing. The4096-column cap was a sampling choice, not a structural saturation theorem.

This follow-up reuses the identical exact predecessor witness, affine-support specialization, complete Gauss-corrected phase specialization, bit-sliced `F_3` accumulation, and elimination. It changes only the deterministic sampled-column horizon to `2^14=16384`.

If rank2048 is reached then

`rank_F3(K_B sampled) <= rank_Q(K_B) <= 2048`

forces exact rational rank2048 for the complete direct-e1 B aggregate at that reachable predecessor, ruling out any uniform subgeneric aggregate-rank upper bound.

If full rank is not reached, the result is only a stronger modular lower bound. No rational upper bound follows.

No complete predecessor-leaf rank, representation-width reduction, arithmetic-work reduction, alpha, or full-round claim is made.
