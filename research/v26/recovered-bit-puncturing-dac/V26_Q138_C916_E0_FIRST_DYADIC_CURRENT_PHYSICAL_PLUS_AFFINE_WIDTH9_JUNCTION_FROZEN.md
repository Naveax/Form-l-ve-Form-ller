# V26 Q138 C916 E0 first-dyadic current physical + affine width-9 junction authority

Status: exact finite certificate, `ALPHA_PASS=0`.

This authority freezes the clean, activity-correct higher-order quotient-layer result for C916 position C. It combines the current 38 exact ternary physical quotient factors, the exact conjunction of the five certified quaternary physical factors, and the complete 19 affine-support obstructions.

## Affine semantics

The affine-support obstruction is an activity relation, not a singleton all-zero tuple. For every certified affine scope, the forbidden set is exactly the Cartesian product of local **nonzero** quotient states. The maximal local quotient-state index is the zero state, and any zero endpoint clears that affine conflict.

## Exact topology

The combined physical + affine primal graph has exact treewidth 9. The deterministic chordal completion used by the verifier has:

- 26 maximal cliques;
- 21 junction-forest edges;
- 5 connected components;
- 13 fill edges;
- largest quotient clique capacity `1,048,576`;
- total raw clique capacity `5,023,824`.

## Frozen exact junction result

Clean sum-product over the compiled junction forest gives component counts

`[37169766629362021600, 63, 61, 77, 62]`

and therefore the exact higher-order assignment count

`681934454409791000768011200`.

Its base-2 logarithm is approximately

`89.13976354526173`.

The compiled tables contain exactly `3,905,754` allowed rows in total. The largest compiled allowed table has `1,048,576` rows, and the largest positive separator message has `262,144` rows.

## Regression authority

The frozen verifier is:

`scripts/verify_v26_q138_c916_e0_first_dyadic_current_physical_plus_affine_junction_frozen_exact.py`

It reruns the merged exact compiler from clean authority artifacts and requires the exact integer, component counts, topology statistics, compiled-row total, and maximum separator support to reproduce exactly.

## Boundary

This is **not** the final weighted C916 count. The dense 4,005 pairwise quotient value relations and multiplicity weights are outside this junction-only count. No end-to-end work exponent, arithmetic-work reduction, ranking gain, or `ALPHA_PASS` follows from this result.

The next exact step is to couple these authority-correct higher-order relations into the exact weighted 4,005-pair recursion while retaining the historical six-ternary + five-quaternary checkpoint as a regression gate.
