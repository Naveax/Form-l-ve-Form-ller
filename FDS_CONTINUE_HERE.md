# FDS_CONTINUE_HERE

**State:** Issue #10 `NO_GO_SIMPLE_8P8_XOR_SEPARABILITY`; Issue #9 constant-factor PASS; alpha still 1.  
**Next:** freeze a separate exact modular-additive half-separability audit.

1. Treat Issue #10 target/split/result as immutable; no second XOR target set.
2. New family must use fresh targets and a separately committed plan.
3. Keep controlled b16 W5_SINGLE 8+8 unless the new plan structurally justifies another scope before results.
4. Test exact modular rectangle residual `S[a,b]-S[a,0]-S[0,b]+S[0,0] mod 2^16`.
5. Primary PASS must imply a concrete half-table matching rule and bounded match count before any MITM scaling claim.
6. Approximate/SVD structure cannot rescue exact modular failure.
7. Only on exact PASS open a fresh TOTAL exponent-scaling protocol.
8. Commit source/tests/results/decision/manifest and continuity.

No alpha<1/full-round claim until explicit TOTAL scaling passes.
