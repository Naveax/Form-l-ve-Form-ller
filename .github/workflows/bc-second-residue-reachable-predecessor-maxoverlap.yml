name: B-C second residue reachable predecessor max-overlap
on:
  pull_request:
    paths:
      - 'scripts/probe_v26_q138_bc_second_residue_reachable_predecessor_geometry.py'
      - 'scripts/probe_v26_q138_bc_second_residue_reachable_predecessor_maxoverlap.py'
      - '.github/workflows/bc-second-residue-reachable-predecessor-maxoverlap.yml'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: bc-second-residue-reachable-predecessor-maxoverlap-${{ github.ref }}
  cancel-in-progress: false
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --quiet sympy==1.14.0 z3-solver==4.15.3.0
      - run: python scripts/probe_v26_q138_bc_second_residue_reachable_predecessor_maxoverlap.py
