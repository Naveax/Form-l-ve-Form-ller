name: C916 quadratic residual separator

on:
  pull_request:
    paths:
      - 'scripts/probe_v26_q138_c916_quadratic_residual_separator.py'
      - 'scripts/probe_v26_q138_c916_quadratic_function_space_separator.py'
      - '.github/workflows/c916-quadratic-residual-separator.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: c916-quadratic-residual-separator-${{ github.ref }}
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --quiet sympy==1.14.0
      - name: Probe C916 quadratic residual separator
        run: python scripts/probe_v26_q138_c916_quadratic_residual_separator.py
