name: B-C e0 reachable joint sign span

on:
  pull_request:
    paths:
      - 'scripts/probe_v26_q138_bc_e0_reachable_joint_sign_span.py'
      - '.github/workflows/bc-e0-reachable-joint-sign-span.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --quiet sympy==1.14.0
      - run: python scripts/probe_v26_q138_bc_e0_reachable_joint_sign_span.py
