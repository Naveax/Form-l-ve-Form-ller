name: C916 e0 width3-base all4-m4 all-order affine

on:
  pull_request:
    paths:
      - 'scripts/probe_v26_q138_c916_e0_first_dyadic_250way_width3_base_all4_m4_all_order_affine.py'
      - '.github/workflows/c916-e0-first-dyadic-250way-width3-base-all4-m4-all-order-affine.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --quiet sympy==1.14.0
      - name: Download frozen PR203 base authority
        uses: actions/download-artifact@v4
        with:
          name: c916-base160-width3-separator-authority
          run-id: 34607657664
          github-token: ${{ github.token }}
          path: authorities/base
      - name: Download frozen PR201 m4 authority
        uses: actions/download-artifact@v4
        with:
          name: c916-four-separator-complete-authority
          run-id: 34605915580
          github-token: ${{ github.token }}
          path: authorities/m4
      - name: Count exact all-order affine m4 separator model
        run: python scripts/probe_v26_q138_c916_e0_first_dyadic_250way_width3_base_all4_m4_all_order_affine.py
