name: C916 E0 tail6 expanded physical topology

on:
  pull_request:
    paths:
      - 'scripts/verify_v26_q138_c916_e0_first_dyadic_tail6_expanded_physical_topology.py'
      - '.github/workflows/c916-e0-first-dyadic-tail6-expanded-physical-topology.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  actions: read

jobs:
  topology:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Syntax-check tail6 topology verifier
        run: python -m py_compile scripts/verify_v26_q138_c916_e0_first_dyadic_tail6_expanded_physical_topology.py
      - uses: actions/download-artifact@v4
        with:
          pattern: c916-tail5-quotient-*
          run-id: 35571898114
          github-token: ${{ github.token }}
          repository: Naveax/Form-l-ve-Form-ller
          path: authorities/tail5-quotient
          merge-multiple: true
      - uses: actions/download-artifact@v4
        with:
          pattern: c916-tail6-quotient-*
          run-id: 35587544692
          github-token: ${{ github.token }}
          repository: Naveax/Form-l-ve-Form-ller
          path: authorities/tail6-quotient
          merge-multiple: true
      - uses: actions/download-artifact@v4
        with:
          name: c916-four-separator-complete-authority
          run-id: 34605915580
          github-token: ${{ github.token }}
          repository: Naveax/Form-l-ve-Form-ller
          path: authorities/m4
      - name: Certify tail6-expanded physical topology
        env:
          C916_TAIL5_QUOTIENT_DIR: authorities/tail5-quotient
          C916_TAIL6_QUOTIENT_DIR: authorities/tail6-quotient
        run: python scripts/verify_v26_q138_c916_e0_first_dyadic_tail6_expanded_physical_topology.py
