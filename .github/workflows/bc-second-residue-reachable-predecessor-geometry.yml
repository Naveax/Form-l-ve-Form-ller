name: B-C second residue reachable predecessor geometry
on:
  pull_request:
    paths:
      - 'scripts/probe_v26_q138_bc_second_residue_reachable_predecessor_geometry.py'
      - '.github/workflows/bc-second-residue-reachable-predecessor-geometry.yml'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: bc-second-residue-reachable-predecessor-geometry-${{ github.ref }}
  cancel-in-progress: false
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --quiet sympy==1.14.0
      - run: python scripts/probe_v26_q138_bc_second_residue_reachable_predecessor_geometry.py
